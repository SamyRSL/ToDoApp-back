spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/springboot_todo_db
    username: app_user
    password: password

  jpa:
    hibernate:
      ddl-auto: create-drop

  sql:
    init:
      mode: never
